resources:
  jobs:
      download_historical_metered_volume_and_pool_price:
      name: download_historical_metered_volume_and_pool_price
      tasks:
        - task_key: download_historical_data
          notebook_task:
            notebook_path: ../jobs/download_historical_metered_volume_and_pool_price
            base_parameters:
              catalog_name: ${var.catalog_name}
              schema_name: alberta_energy
              volume_name: aeso_ingest